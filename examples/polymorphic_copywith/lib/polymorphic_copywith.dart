import 'package:polymorphic_copywith/models/one.dart';
import 'package:polymorphic_copywith/models/two.dart';

void main() {
  final one = One(id: 'one', name: 'one', objects: {});
  final two = Two(id: 'two');
}
